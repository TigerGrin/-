name: Build and Push to GitHub Container Registry

# 使用 GitHub Container Registry (ghcr.io)
# 优点：免费、无需额外账号、国内可访问、无需配置 Secrets
# 首次使用后，需要将镜像设置为公开（Public）

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

env:
  IMAGE_NAME: hospital-schedule-system
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Output image info
        if: github.event_name != 'pull_request' && github.event_name != 'pull_request_target'
        run: |
          echo "✅ 镜像构建成功！"
          echo "📦 镜像地址：${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          echo "🏷️  标签：${{ steps.meta.outputs.tags }}"
          echo ""
          echo "在 Sealos 应用部署中使用以下镜像地址："
          echo "${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest"
          echo ""
          echo "⚠️  首次使用需要将镜像设置为公开："
          echo "访问：https://github.com/${{ github.repository_owner }}?tab=packages"
          echo "找到 hospital-schedule-system 包 → Package settings → Change visibility → Make public"

